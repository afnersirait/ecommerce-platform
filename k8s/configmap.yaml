apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-config
  namespace: ecommerce
data:
  NODE_ENV: "production"
  PORT: "5000"
  CLIENT_URL: "https://your-domain.com"
  # Add other non-sensitive configuration here
